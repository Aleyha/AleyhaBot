const ERROR_CONSTANTS = {
    INVALID_REQUEST_PARAM: 'Invalid request param length',
    FAILED_AUTH_TOKEN: 'Unable to load token from auth.json',
    INVALID_REQUEST: 'Invalid request: '
};

module.exports = ERROR_CONSTANTS;